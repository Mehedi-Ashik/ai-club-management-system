from django.urls import path
from . import views

app_name = "ai_features"

urlpatterns = [
    # path("generate-event-description/", views.generate_event_description, name="generate_event_description"),
]
